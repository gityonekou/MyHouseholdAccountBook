# マイ家計簿アプリケーション 現状クラス図

**作成日**: 2025年11月15日

---

## 1. 全体アーキテクチャ図

```mermaid
graph TB
    subgraph Presentation["Presentation Layer"]
        Controller[Controllers]
        Request[Request DTOs]
        Response[Response DTOs]
    end
    
    subgraph Application["Application Layer"]
        UseCase[UseCases]
    end
    
    subgraph Domain["Domain Layer"]
        Model[Domain Models]
        Repository[Repositories<br/>Interfaces]
        Type[Value Objects]
    end
    
    subgraph Infrastructure["Infrastructure Layer"]
        DataSource[DataSources<br/>Repository Impl]
        DTO[DTOs]
        Mapper[MyBatis Mappers]
    end
    
    Controller --> UseCase
    UseCase --> Model
    UseCase --> Repository
    Model --> Type
    Repository -.implements.-> DataSource
    DataSource --> Mapper
    DataSource --> DTO
    
    style Presentation fill:#e1f5ff
    style Application fill:#fff4e1
    style Domain fill:#e8f5e9
    style Infrastructure fill:#f3e5f5
```

---

## 2. 主要クラス図：月次収支照会機能

### 2.1 クラス関係図

```mermaid
classDiagram
    %% Presentation Layer
    class AccountMonthController {
        -AccountMonthInquiryUseCase useCase
        +getAccountMonthInquiry() String
        +getAccountMonthInquiryWithTargetMonth() String
    }
    
    class AccountMonthInquiryResponse {
        -String targetYearMonth
        -String syuunyuuKingaku
        -String sisyutuKingaku
        -List~ExpenditureListItem~ expenditureItemList
        +getInstance() AccountMonthInquiryResponse
        +addExpenditureItemList()
    }
    
    %% Application Layer
    class AccountMonthInquiryUseCase {
        -AccountBookUserInquiryUseCase userInquiry
        -SisyutuKingakuTableRepository repository
        -IncomeAndExpenditureTableRepository syuusiRepository
        +read(LoginUserInfo) AccountMonthInquiryResponse
        +read(LoginUserInfo, String) AccountMonthInquiryResponse
        -execRead() AccountMonthInquiryResponse
        -convertExpenditureItemList() List
    }
    
    %% Domain Layer - Models
    class IncomeAndExpenditureItem {
        -UserId userId
        -TargetYear targetYear
        -TargetMonth targetMonth
        -SyuunyuuKingaku syuunyuuKingaku
        -WithdrewKingaku withdrewKingaku
        -SisyutuKingaku sisyutuKingaku
        -SyuusiKingaku syuusiKingaku
        +from() IncomeAndExpenditureItem
        +isEmpty() boolean
        +addSisyutuKingaku() IncomeAndExpenditureItem
    }
    
    class AccountMonthInquiryExpenditureItemList {
        -List~SisyutuKingakuItem~ values
        +from() AccountMonthInquiryExpenditureItemList
        +isEmpty() boolean
        +getValues() List
    }
    
    class SisyutuKingakuItem {
        -SisyutuItemCode sisyutuItemCode
        -SisyutuItemName sisyutuItemName
        -SisyutuItemLevel sisyutuItemLevel
        -SisyutuKingaku sisyutuKingaku
        -ShiharaiDate shiharaiDate
        +from() SisyutuKingakuItem
    }
    
    %% Domain Layer - Value Objects
    class UserId {
        -String value
        +from(String) UserId
        +getValue() String
    }
    
    class SyuunyuuKingaku {
        -BigDecimal value
        +from(BigDecimal) SyuunyuuKingaku
        +getValue() BigDecimal
        +toString() String
    }
    
    class SisyutuKingaku {
        -BigDecimal value
        +from(BigDecimal) SisyutuKingaku
        +add(SisyutuKingaku) SisyutuKingaku
        +subtract(SisyutuKingaku) SisyutuKingaku
        +getValue() BigDecimal
    }
    
    class TargetYearMonth {
        -String value
        +from(String) TargetYearMonth
        +getYear() String
        +getMonth() String
    }
    
    %% Domain Layer - Repositories
    class IncomeAndExpenditureTableRepository {
        <<interface>>
        +select(SearchQuery) IncomeAndExpenditureItem
        +insert(IncomeAndExpenditureItem) int
        +update(IncomeAndExpenditureItem) int
    }
    
    class SisyutuKingakuTableRepository {
        <<interface>>
        +select(SearchQuery) AccountMonthInquiryExpenditureItemList
        +update(SisyutuKingakuItem) int
    }
    
    %% Infrastructure Layer
    class IncomeAndExpenditureTableDataSource {
        -IncomeAndExpenditureTableMapper mapper
        +select(SearchQuery) IncomeAndExpenditureItem
        +insert(IncomeAndExpenditureItem) int
    }
    
    class SisyutuKingakuTableDataSource {
        -SisyutuKingakuTableMapper mapper
        +select(SearchQuery) AccountMonthInquiryExpenditureItemList
    }
    
    class IncomeAndExpenditureTableMapper {
        <<MyBatis Mapper>>
        +findByYearMonth() IncomeAndExpenditureDto
        +insert() int
    }
    
    %% Relationships
    AccountMonthController --> AccountMonthInquiryUseCase
    AccountMonthController --> AccountMonthInquiryResponse
    AccountMonthInquiryUseCase --> IncomeAndExpenditureTableRepository
    AccountMonthInquiryUseCase --> SisyutuKingakuTableRepository
    AccountMonthInquiryUseCase --> IncomeAndExpenditureItem
    AccountMonthInquiryUseCase --> AccountMonthInquiryExpenditureItemList
    
    IncomeAndExpenditureItem --> UserId
    IncomeAndExpenditureItem --> SyuunyuuKingaku
    IncomeAndExpenditureItem --> SisyutuKingaku
    IncomeAndExpenditureItem --> TargetYearMonth
    
    AccountMonthInquiryExpenditureItemList --> SisyutuKingakuItem
    SisyutuKingakuItem --> SisyutuKingaku
    
    IncomeAndExpenditureTableRepository <|.. IncomeAndExpenditureTableDataSource
    SisyutuKingakuTableRepository <|.. SisyutuKingakuTableDataSource
    IncomeAndExpenditureTableDataSource --> IncomeAndExpenditureTableMapper
```

---

## 3. 主要クラス図：買い物登録機能

### 3.1 クラス関係図

```mermaid
classDiagram
    %% Presentation Layer
    class ShoppingRegistController {
        -ShoppingRegistUseCase useCase
        +getShoppingRegist() String
        +postShoppingRegist() String
    }
    
    class ShoppingRegistRequest {
        -String targetYearMonth
        -String shopCode
        -String shoppingDate
        -List~ShoppingRegistItem~ itemList
        +validate()
    }
    
    %% Application Layer
    class ShoppingRegistUseCase {
        -ShoppingRegistTableRepository repository
        -ExpenditureTableRepository expenditureRepository
        +read() ShoppingRegistResponse
        +execAction() AbstractResponse
        -validateShoppingData()
        -calculateTotalAmount()
    }
    
    %% Domain Layer - Models
    class ShoppingRegistItem {
        -ShoppingItemCode itemCode
        -ShoppingItemName itemName
        -ShopCode shopCode
        -Quantity quantity
        -UnitPrice unitPrice
        -TotalAmount totalAmount
        +from() ShoppingRegistItem
        +calculateTotalAmount() TotalAmount
    }
    
    class ExpenditureItem {
        -UserId userId
        -TargetYearMonth targetYearMonth
        -ExpenditureCode expenditureCode
        -SisyutuItemCode sisyutuItemCode
        -ExpenditureName expenditureName
        -ExpenditureKingaku expenditureKingaku
        -ShiharaiDate shiharaiDate
        +from() ExpenditureItem
        +isValid() boolean
    }
    
    %% Domain Layer - Value Objects
    class ShoppingItemCode {
        -String value
        +from(String) ShoppingItemCode
        +getValue() String
    }
    
    class Quantity {
        -int value
        +from(int) Quantity
        +getValue() int
        +multiply(UnitPrice) TotalAmount
    }
    
    class UnitPrice {
        -BigDecimal value
        +from(BigDecimal) UnitPrice
        +getValue() BigDecimal
    }
    
    class TotalAmount {
        -BigDecimal value
        +from(BigDecimal) TotalAmount
        +add(TotalAmount) TotalAmount
    }
    
    %% Domain Layer - Repositories
    class ShoppingRegistTableRepository {
        <<interface>>
        +insert(ShoppingRegistItem) int
        +selectByYearMonth() List~ShoppingRegistItem~
    }
    
    class ExpenditureTableRepository {
        <<interface>>
        +select(SearchQuery) ExpenditureItem
        +insert(ExpenditureItem) int
        +sumExpenditureKingaku() SisyutuKingakuTotalAmount
    }
    
    %% Relationships
    ShoppingRegistController --> ShoppingRegistUseCase
    ShoppingRegistController --> ShoppingRegistRequest
    ShoppingRegistUseCase --> ShoppingRegistTableRepository
    ShoppingRegistUseCase --> ExpenditureTableRepository
    ShoppingRegistUseCase --> ShoppingRegistItem
    ShoppingRegistUseCase --> ExpenditureItem
    
    ShoppingRegistItem --> ShoppingItemCode
    ShoppingRegistItem --> Quantity
    ShoppingRegistItem --> UnitPrice
    ShoppingRegistItem --> TotalAmount
    
    ExpenditureItem --> UserId
    ExpenditureItem --> TargetYearMonth
```

---

## 4. ドメインモデル継承階層

### 4.1 値オブジェクト階層

```mermaid
classDiagram
    %% 共通基底
    class DomainPrimitive {
        <<abstract>>
        +equals() boolean
        +hashCode() int
    }
    
    %% 金額系
    class MoneyValue {
        <<abstract>>
        -BigDecimal value
        +getValue() BigDecimal
        +add() MoneyValue
        +subtract() MoneyValue
    }
    
    class SyuunyuuKingaku
    class SisyutuKingaku
    class WithdrewKingaku
    class SyuusiKingaku
    class ExpenditureKingaku
    
    %% 日付系
    class DateValue {
        <<abstract>>
        -LocalDate value
        +getValue() LocalDate
        +isBefore() boolean
        +isAfter() boolean
    }
    
    class ShiharaiDate
    class EventStartDate
    class EventEndDate
    
    %% コード系
    class CodeValue {
        <<abstract>>
        -String value
        +getValue() String
    }
    
    class UserId
    class SisyutuItemCode
    class ShopCode
    class EventCode
    
    %% 名称系
    class NameValue {
        <<abstract>>
        -String value
        +getValue() String
    }
    
    class SisyutuItemName
    class ShopName
    class EventName
    
    %% 継承関係
    MoneyValue --|> DomainPrimitive
    DateValue --|> DomainPrimitive
    CodeValue --|> DomainPrimitive
    NameValue --|> DomainPrimitive
    
    SyuunyuuKingaku --|> MoneyValue
    SisyutuKingaku --|> MoneyValue
    WithdrewKingaku --|> MoneyValue
    SyuusiKingaku --|> MoneyValue
    ExpenditureKingaku --|> MoneyValue
    
    ShiharaiDate --|> DateValue
    EventStartDate --|> DateValue
    EventEndDate --|> DateValue
    
    UserId --|> CodeValue
    SisyutuItemCode --|> CodeValue
    ShopCode --|> CodeValue
    EventCode --|> CodeValue
    
    SisyutuItemName --|> NameValue
    ShopName --|> NameValue
    EventName --|> NameValue
```

---

## 5. リポジトリパターン構造

```mermaid
classDiagram
    %% Domain Layer - Repository Interfaces
    class Repository {
        <<interface>>
    }
    
    class IncomeAndExpenditureTableRepository {
        <<interface>>
        +select() IncomeAndExpenditureItem
        +insert() int
        +update() int
    }
    
    class ExpenditureTableRepository {
        <<interface>>
        +select() ExpenditureItem
        +insert() int
        +sumExpenditureKingaku() SisyutuKingakuTotalAmount
    }
    
    class SisyutuItemTableRepository {
        <<interface>>
        +select() SisyutuItem
        +insert() int
        +update() int
    }
    
    %% Infrastructure Layer - Repository Implementations
    class IncomeAndExpenditureTableDataSource {
        -IncomeAndExpenditureTableMapper mapper
        +select() IncomeAndExpenditureItem
        +insert() int
    }
    
    class ExpenditureTableDataSource {
        -ExpenditureTableMapper mapper
        +select() ExpenditureItem
        +insert() int
    }
    
    class SisyutuItemTableDataSource {
        -SisyutuItemTableMapper mapper
        +select() SisyutuItem
        +insert() int
    }
    
    %% MyBatis Mappers
    class IncomeAndExpenditureTableMapper {
        <<MyBatis Mapper>>
        +findByYearMonth() IncomeAndExpenditureDto
        +insert() int
    }
    
    class ExpenditureTableMapper {
        <<MyBatis Mapper>>
        +findByCondition() ExpenditureDto
        +insert() int
    }
    
    class SisyutuItemTableMapper {
        <<MyBatis Mapper>>
        +findByCode() SisyutuItemDto
        +insert() int
    }
    
    %% Relationships
    Repository <|-- IncomeAndExpenditureTableRepository
    Repository <|-- ExpenditureTableRepository
    Repository <|-- SisyutuItemTableRepository
    
    IncomeAndExpenditureTableRepository <|.. IncomeAndExpenditureTableDataSource
    ExpenditureTableRepository <|.. ExpenditureTableDataSource
    SisyutuItemTableRepository <|.. SisyutuItemTableDataSource
    
    IncomeAndExpenditureTableDataSource --> IncomeAndExpenditureTableMapper
    ExpenditureTableDataSource --> ExpenditureTableMapper
    SisyutuItemTableDataSource --> SisyutuItemTableMapper
```

---

## 6. 現状の問題点（クラス図からの考察）

### 6.1 ユースケース層の肥大化

```mermaid
graph LR
    subgraph "現状（問題あり）"
        UC1[UseCase]
        UC1 --> BL1[業務ロジック]
        UC1 --> VL1[バリデーション]
        UC1 --> CV1[変換処理]
        UC1 --> DC1[データチェック]
    end
    
    subgraph "理想（DDD適用後）"
        UC2[UseCase]
        DM[Domain Model]
        DS[Domain Service]
        
        UC2 --> DM
        UC2 --> DS
        DM --> BL2[業務ロジック]
        DM --> VL2[バリデーション]
        DS --> DC2[データ整合性]
    end
    
    style UC1 fill:#ffcccc
    style UC2 fill:#ccffcc
```

### 6.2 ドメインモデルの貧弱性

**現状の問題**:
- ドメインモデルはデータ保持が主
- 業務ロジックがユースケース層に散在
- ドメインサービスが不足

**改善後の姿**:
- ドメインモデルに業務ロジックを集約
- ドメインサービスで複数エンティティにまたがるロジックを管理
- 集約による整合性保証

---

## 7. まとめ

### 現状の特徴

✅ **良い点**:
- レイヤー分離が明確
- 値オブジェクトを活用
- リポジトリパターンの採用
- 不変オブジェクト設計

⚠️ **改善点**:
- ユースケース層への業務ロジック集中
- ドメインモデルの貧弱性（貧血ドメインモデル）
- 集約の境界が不明確
- ドメインサービスの不足

### リファクタリングの方向性

1. **ドメインモデルの強化**
   - エンティティに業務ロジックを移動
   - 集約の明確化
   - ドメインサービスの抽出

2. **ユースケースの責任縮小**
   - オーケストレーション role に専念
   - ドメインモデルへの委譲

3. **値オブジェクトの充実**
   - バリデーションロジックの内包
   - ドメイン計算の実装

---

**次のドキュメント**: 
- [ER図](./03_ER図.md)
- [要件定義書](./04_要件定義書.md)

---

**作成者**: Claude (Anthropic)  
**最終更新**: 2025年11月15日
